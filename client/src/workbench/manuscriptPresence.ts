/** Shared list/inspector copy. Presence is this chip, not a Progress count. */
export function leftTheManuscriptLabel(): string {
  return 'Left the manuscript'
}

export function leftTheManuscriptTitle(): string {
  return 'This remark is no longer in the .tex file.'
}
